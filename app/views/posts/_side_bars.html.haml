%aside.post-aside-1
  %h4.post-feed-heading.heading-background.heading-center Senaste inläggen

  %nav
    %ol
      - latest_posts.each do |post|
        %li.post
          %h4.post-latest-link= link_to(post.title,post_path(post))

%aside.post-aside-2
  .post-admin
    %h2.post-admin-heading.heading-background.heading-center Administration
    %nav
      %ol
        %li
          %h4.post-latest-link= link_to('Ny post', new_post_path, :class => "button add")
        - if local_assigns[:post]
          %li
            %h4.post-latest-link= link_to('Uppdatera post', edit_post_path(post), :class => "button add")
